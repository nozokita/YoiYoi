import SwiftUI

/// DESIGN.md v5.0「カラーパレット」と一致。
enum AppColors {
    static let coralRed = Color(hex: "#F26B6B")
    static let coralLight = Color(hex: "#FF8A8A")
    static let coralDeep = Color(hex: "#E05A5A")
    static let mintGreen = Color(hex: "#8EDCD0")
    static let mintLight = Color(hex: "#B8EDE5")
    static let sunnyYellow = Color(hex: "#FFD93D")
    static let yellowLight = Color(hex: "#FFEFB0")
    static let cream = Color(hex: "#FFF8F0")
    static let pureWhite = Color(hex: "#FFFFFF")
    static let charcoal = Color(hex: "#2D3436")
    static let greyText = Color(hex: "#636E72")
    static let warmCoral = Color(hex: "#E17055")
    static let amber80 = Color(hex: "#FDCB6E")
    static let darkBg = Color(hex: "#1A1A2E")
    static let lavender = Color(hex: "#B39DDB")
    /// 設定ヒーロー下部（DESIGN.md ヒーロー配色表）。
    static let lavenderLight = Color(hex: "#E1BEE7")
}
