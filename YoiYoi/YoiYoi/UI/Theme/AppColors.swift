import SwiftUI

/// DESIGN.md v3.0「カラーパレット」と一致。
enum AppColors {
    static let coralRed = Color(hex: "#D95F59")
    static let coralLight = Color(hex: "#F9E2DC")
    static let coralDeep = Color(hex: "#A9443F")
    static let mintGreen = Color(hex: "#4FAF9F")
    static let mintLight = Color(hex: "#E3F3EE")
    static let sunnyYellow = Color(hex: "#DFAE38")
    static let yellowLight = Color(hex: "#FFF2CB")
    static let cream = Color(hex: "#F7F2EA")
    static let pureWhite = Color(hex: "#FFFEFB")
    static let charcoal = Color(hex: "#202733")
    static let greyText = Color(hex: "#667085")
    static let warmCoral = Color(hex: "#C85F43")
    static let amber80 = Color(hex: "#C88B2B")
    static let darkBg = Color(hex: "#182236")
    static let lavender = Color(hex: "#7C6FA8")
    static let lavenderLight = Color(hex: "#EDE9F7")

    static let navy = Color(hex: "#25324A")
    static let surface = Color(hex: "#FBF7F1")
    static let surfaceElevated = Color(hex: "#FFFEFB")
    static let hairline = Color(hex: "#E8DED2")
    static let inkMuted = Color(hex: "#475467")
    static let successDeep = Color(hex: "#2F7D72")
}
